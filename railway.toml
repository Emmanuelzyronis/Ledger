[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'LEDGER_PORT=$PORT LEDGER_HOST=0.0.0.0 python -m ledger'"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
