[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src LEDGER_PORT=$PORT LEDGER_HOST=0.0.0.0 uvicorn ledger.service:create_app --factory --host 0.0.0.0 --port $PORT"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
